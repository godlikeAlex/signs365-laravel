import { useAppSelector } from "@/src/hooks";
import { login } from "@/src/redux/authSlice";
import React from "react";
import { useDispatch } from "react-redux";

export default function Home() {
  return <h1>Homiesss</h1>;
}
